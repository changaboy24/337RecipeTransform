337RecipeTransform
==================

Running with Python 2.7.5

Python libraries use:

pymongo
	pip install pymongo

mongodb
	brew install mongodb

nltk, pyyaml
	pip install -U pyyaml nltk

gspread
	pip install gspread